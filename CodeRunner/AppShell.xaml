<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CodeRunner.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:CodeRunner"
    Shell.FlyoutBehavior="Disabled"
    Shell.TabBarIsVisible="False"
    Shell.NavBarIsVisible="False"
    Shell.FlyoutItemIsVisible="False"
    Title="Code Runner">

    <ShellContent
        Title="Lobby"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    <ShellContent Title="Game" Route="GamePage" ContentTemplate="{DataTemplate local:GamePage}" />
    <ShellContent Title="Settings" Route="SettingsPage" ContentTemplate="{DataTemplate local:SettingsPage}" />

</Shell>
